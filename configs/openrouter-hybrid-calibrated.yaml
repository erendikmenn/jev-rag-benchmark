extends: openrouter-hybrid.yaml

run:
  deduplicate_queries: true

rerankers:
  jev:
    threshold: 0.50
    threshold_source: dev
    calibration_report: reports/calibration/jev-threshold-xquad-tr-hybrid.json
