extends: default.yaml
run:
  seed: 20260919
  concurrency: 1
  cache: warm
  # User-approved ceiling; expected spend is far below this.
  max_budget_usd: 5.00
  latency_repetitions: 1
  deduplicate_queries: true
